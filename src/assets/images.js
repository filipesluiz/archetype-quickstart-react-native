const react = require('./img/react_logo.png');
const home = require('./img/home.png');
const settings = require('./img/settings.png');

const Images = {
  react,
  home,
  settings,
};

export default Images;
